import { ExternalLinksPipe } from './external-links.pipe';

describe('ExternalLinksPipe', () => {
  it('create an instance', () => {
    const pipe = new ExternalLinksPipe();
    expect(pipe).toBeTruthy();
  });
});

import { ServerSideValidationDirective } from './server-side-validation.directive';

describe('ServerSideValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new ServerSideValidationDirective();
    expect(directive).toBeTruthy();
  });
});

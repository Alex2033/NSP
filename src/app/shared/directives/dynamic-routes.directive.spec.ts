import { DynamicRoutesDirective } from './dynamic-routes.directive';

describe('DynamicRoutesDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicRoutesDirective();
    expect(directive).toBeTruthy();
  });
});
